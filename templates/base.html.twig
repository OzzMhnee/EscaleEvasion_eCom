<!DOCTYPE html>
<html>
	<head>
		<meta charset="UTF-8">
		<title>
			{% block title %}My Shope{% endblock %}
		</title>
		<link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">
		<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-LN+7fdVzj6u52u30Kp6M/trliBMCMKTyK833zpbD+pXdCLuTusPj697FH4R/5mcr" crossorigin="anonymous"> {% block stylesheets %}{% endblock %}
		<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/js/bootstrap.bundle.min.js" integrity="sha384-ndDqU0Gzau9qJ1lfW4pNLlhNTkCfHzAVBReH9diLvGRem5+R9g2FzA8ZGN954O5Q" crossorigin="anonymous"></script>
		<!-- Importmap for JavaScript modules -->
		{% block javascripts %}
			{% block importmap %}
				{{ importmap('app') }}
			{% endblock %}
		{% endblock %}
		<script> // Gestion des backgrounds
			document.addEventListener('DOMContentLoaded', function() {
				const backgrounds = [
					'/protected-bg/backgroundParallax.png',
					'/protected-bg/backgroundParallax2.png',
					'/protected-bg/backgroundParallax4.png',
					'/protected-bg/backgroundParallax5.png',
					'/protected-bg/backgroundParallax6.png'
					//....
				];
				const idx = Math.floor(Math.random() * backgrounds.length);
				// Application à tous les éléments .parallax
				document.querySelectorAll('.parallax').forEach(function(el) {
					el.style.backgroundImage = "url('" + backgrounds[idx] + "')";
				});
			});
			// document.addEventListener('DOMContentLoaded', disableImageProtection);
		</script>
	</head>
	
	<script> // Désactive le clic droit sur les images
		// Pour empêcher le clic droit et le glisser-déposer sur les images
		// Cela peut être utilisé pour protéger les images contre le téléchargement direct
		// Note: Ce n'est pas une protection infaillible, mais cela peut décourager certains utilisateurs
		document.addEventListener('DOMContentLoaded', function() {
			document.querySelectorAll('img').forEach(function(img) {
			img.addEventListener('contextmenu', function(e) {
				e.preventDefault();
			});
			img.addEventListener('dragstart', function(e) {
				e.preventDefault();
			});
			});
		});
	</script>
	
	<body>
		{% block nav %}
			{% include 'layouts/navbar.html.twig' %}
		{% endblock %}

		{% block flash_message %}
			{% include 'layouts/flash_message.html.twig' %}
		{% endblock %}

		{% block body %} {% endblock %}
		
		{% block footer %}
			{% include 'layouts/footer.html.twig' %}
		{% endblock %}
	</body>
</html>
